name: litgraph-local

volumes:
  frontend_node_modules:

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports: ["6333:6333", "6334:6334"]
    volumes:
      - ./qdrant/data:/qdrant/storage
    profiles: ["core"]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]
    profiles: ["redis-local"]

  api:
    build:
      context: ..
      dockerfile: ./backend/Dockerfile
      target: cpu   # Use the cpu stage
    working_dir: /app
    volumes:
      - ../backend:/app
      - /mnt/d/colab/llms/bge-m3:/models/bge-m3:ro
    env_file:
      - ./qdrant/.env
      - ./app/.env
    environment:
      - EMBEDDING_MODEL_PATH=/models/bge-m3
    depends_on:
      qdrant:
        condition: service_started
      redis:
        condition: service_started
    command: ["uv", "run", "poe", "server"]
    profiles: ["app"]

  worker:
    build:
      context: ..
      dockerfile: ./backend/Dockerfile
      target: cpu   # Use the cpu
    working_dir: /app
    volumes:
      - ../backend:/app
      - /mnt/d/colab/llms/bge-m3:/models/bge-m3:ro
    env_file:
      - ./qdrant/.env
      - ./app/.env
    environment:
      - EMBEDDING_MODEL_PATH=/models/bge-m3
    depends_on:
      qdrant:
        condition: service_started
      redis:
        condition: service_started
    command: ["uv", "run", "--", "python", "-c", "from src.worker import run_worker_loop; run_worker_loop()"]
    profiles: ["worker"]

  # DEV: Vite + HMR
  frontend-dev:
    build:
      context: ..
      dockerfile: ./frontend/Dockerfile
      target: dev
    working_dir: /app
    volumes:
      - ../frontend:/app
      - frontend_node_modules:/app/node_modules
    ports:
      - "5173:5173"
    command: ["bun", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
    depends_on:
      api:
        condition: service_started
    profiles: ["frontend-dev"]

  mcp:
    build:
      context: ..
      dockerfile: ./backend/Dockerfile
      target: cpu
    working_dir: /app
    volumes:
      - ../backend:/app
      - ../docs:/docs:ro
      - /mnt/d/colab/llms/bge-m3:/models/bge-m3:ro
      - /mnt/d/colab/llms/gemma-3-4b-it:/models/gemma-3-4b-it:ro
      - /mnt/d/colab/llms/jina-reranker-v3:/models/jina-reranker-v3:ro
    env_file:
      - ./qdrant/.env
      - ./app/.env
    environment:
      - EMBEDDING_MODEL_PATH=/models/bge-m3
      - EMBEDDING_DIM=1024
      - LLM_MODEL_PATH=/models/gemma-3-4b-it
      - RERANKER_MODEL_PATH=/models/jina-reranker-v3
    depends_on:
      qdrant:
        condition: service_started
      redis:
        condition: service_started
    command: ["uv", "run", "python", "src/mcp_server.py"]
    ports: ["8888:8888"]
    profiles: ["app"]

  # PROD: nginx serving built assets
  frontend-prod:
    build:
      context: ..
      dockerfile: ./frontend/Dockerfile
      target: prod
    ports:
      - "8080:80"
    depends_on:
      api:
        condition: service_started
    profiles: ["frontend-prod"]
